{
  "name": "trello-scope-tracker",
  "version": "1.0.0",
  "description": "## Description",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "export SLS_DEBUG=* && node --inspect /usr/local/bin/serverless offline -s dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-offline": "^4.10.5"
  },
  "dependencies": {
    "node-fetch": "^2.6.0",
    "trello": "^0.9.1"
  }
}
